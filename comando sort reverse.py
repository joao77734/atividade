lista = [0,1,2,43,7,56,76]
lista.sort (reverse=True)
print(lista)

#quando usamos o seguinte código: lista.sort (reverse=True), a ordem dos números ficam em ordem decrescente. OBS= reverse já é um comando de categoria False, para fazê-lo tornar true, é só botar ""=True" no final.